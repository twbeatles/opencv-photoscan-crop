# Photo Cropper 기능 구현 점검 리포트

- 점검일: 2026-03-01
- 점검 기준 문서:
  - `README.md` (프로젝트 루트)
  - `;opencv/CLAUDE.md`
- 점검 범위:
  - `;opencv/photo_cropper/core/*`
  - `;opencv/photo_cropper/ui/*`
  - `;opencv/photo_cropper/utils/*`
  - `;opencv/photo_cropper/cli.py`

## 핵심 발견사항 (구현 리스크)

### 1) 수동 크롭 적용 시 런타임 ImportError 가능성 (높음)
- 위치: `;opencv/photo_cropper/ui/widgets/crop_editor_widget.py:647`
- 내용:
  - `from ..core.advanced_processing import AdvancedImageProcessor`
  - 현재 모듈 경로(`photo_cropper.ui.widgets`) 기준으로 `..core`는 `photo_cropper.ui.core`를 가리켜 잘못된 상대 경로입니다.
- 영향:
  - 수동 크롭/원근 보정 적용 시점에 예외가 발생하여 기능이 실패할 수 있습니다.
- 권장:
  - `from ...core.advanced_processing import AdvancedImageProcessor`로 수정 필요.

### 2) CLI `--preset` 옵션이 선언만 되고 실제 반영되지 않음 (높음)
- 위치:
  - 선언: `;opencv/photo_cropper/cli.py:220`
  - 처리 로직: `;opencv/photo_cropper/cli.py:311` 이후 (`args.preset` 사용 없음)
- 영향:
  - 사용자/자동화 스크립트가 `--preset`을 전달해도 동작 변화가 없습니다.
  - 도움말/예시와 실제 구현이 불일치합니다.
- 권장:
  - `BatchProfileManager` 연동 또는 옵션 제거/명확한 경고 출력 중 하나로 정리.

### 3) CLI `--config`가 대부분의 설정을 무시함 (중간~높음)
- 위치:
  - config 로드: `;opencv/photo_cropper/cli.py:350-352`
  - 실제 적용: `;opencv/photo_cropper/cli.py:355-358` (`canny_min`, `canny_max`만 사용)
- 영향:
  - config 파일을 넣어도 대부분의 필드(출력, 워터마크, 리사이즈, AI 옵션 등)가 반영되지 않습니다.
  - 운영 환경에서 설정 기반 배치 자동화가 기대대로 동작하지 않을 수 있습니다.
- 권장:
  - `AppSettings.from_dict()` 기반으로 먼저 로드하고, CLI 인자는 override 하도록 병합 구조로 변경.

### 4) 기본 프로파일 키 불일치로 고급 설정 미적용 (중간)
- 위치:
  - 프로파일 데이터: `;opencv/photo_cropper/core/batch_profile_manager.py:113` (`advanced_processing`)
  - 적용 로직: `;opencv/photo_cropper/core/batch_profile_manager.py:497-505` (`hasattr(settings, key)`)
  - 실제 설정 필드: `;opencv/photo_cropper/core/settings.py` (`AppSettings.advanced`)
- 영향:
  - 기본 프로파일(예: "오래된 앨범")의 일부 고급 보정 설정이 적용되지 않습니다.
- 권장:
  - 키를 `advanced`로 맞추거나, 구버전 키 매핑 레이어 추가.

### 5) Watch Mode 재귀 감시에서 신규 하위폴더 초기 파일 누락 가능성 (중간)
- 위치:
  - 하위 디렉토리 감시 추가: `;opencv/photo_cropper/core/folder_watcher.py:251-267`
  - 변경 감지 스캔: `;opencv/photo_cropper/core/folder_watcher.py:211-246`
- 내용:
  - 새 하위 디렉토리를 watcher에 추가만 하고, 그 디렉토리의 기존 파일 스캔은 수행하지 않습니다.
- 영향:
  - 폴더 단위 복사(이미 파일이 들어있는 디렉토리 유입) 시 일부 이미지가 처리되지 않을 수 있습니다.
- 권장:
  - 신규 디렉토리 추가 직후 `_check_for_new_files(dir_path)` 또는 재귀 스캔 수행.

### 6) Watch Mode 파일 안정화 대기시간 하드코딩(30초)로 대용량 입력 실패 가능성 (중간)
- 위치: `;opencv/photo_cropper/core/folder_watcher.py:375`, `:541-543`
- 영향:
  - 느린 네트워크 드라이브/대용량 TIFF 입력 시 30초 초과하면 실패 처리되어 자동 처리 누락 가능.
- 권장:
  - 설정값(`watch_mode.max_wait_s`)으로 외부화하고, 재시도 횟수/백오프 추가.

## 추가하면 좋은 기능/보강 포인트

### A) CLI 기능 완성도 보강
- 추천:
  - `--preset` 실제 연동 (`BatchProfileManager` 활용)
  - 분류/얼굴감지/스마트보정 등 v9 핵심 옵션 CLI 노출
  - `--config` + CLI override 우선순위 명확화

### B) Watch Mode 관측성 강화
- 추천:
  - "누락/실패 이유"를 파일 단위로 상태창/로그에 구분 출력 (not ready timeout, permission, decode fail 등)
  - 큐 길이와 평균 대기시간 지표 추가

### C) 회귀 테스트 보강
- 추천 테스트:
  - 수동 크롭 위젯 import/적용 smoke test
  - 재귀 감시에서 "하위 폴더 통째 유입" 케이스
  - CLI `--preset`, `--config` 동작 검증

## 우선순위 제안

1. `crop_editor_widget` 잘못된 import 경로 수정 (즉시)
2. CLI `--preset`/`--config` 동작 일치화 (단기)
3. Watch Mode 하위폴더 초기 스캔 + max wait 설정화 (단기)
4. 프로파일 키 호환(`advanced_processing` -> `advanced`) 정리 (단기)

## 점검 중 확인된 실행 환경 제한

- `python -m photo_cropper.selftest` 실행 시 `No module named 'cv2'`로 중단됨.
- 따라서 런타임 재현 테스트는 현 환경에서 완전 수행하지 못했고, 본 리포트는 정적 코드 검토 중심입니다.


## 구현 완료 상태 (2026-03-01)

### 완료 항목
- [x] 리포트 1) 수동 크롭 import 경로 오류 수정
- [x] 리포트 2) CLI --preset 실동작 연결
- [x] 리포트 3) CLI --config 전체 설정 병합 반영
- [x] 리포트 4) 프로파일 레거시 키(dvanced_processing) 읽기 호환 + 저장 표준화(dvanced)
- [x] 리포트 5) Watch 재귀 감시 시 신규 하위 폴더 초기 스캔 보강
- [x] 리포트 6) Watch max wait 설정화(watch_mode.max_wait_seconds, 기본 30.0)
- [x] 추가 A) CLI AI 핵심 옵션 노출/검증
- [x] 추가 B) Watch 상세 상태/사유/큐 메트릭 관측성 강화
- [x] 추가 C) selftest 보강(수동 크롭 import, CLI 병합 우선순위, 재귀 유입, max wait roundtrip)

### 검증 결과
- python -m py_compile 대상 파일 문법 검증 통과
- python -m photo_cropper.cli --help에서 신규 옵션 노출 확인
- 선택 selftest(CLI 병합, 재귀 감시 유입, max wait roundtrip) 통과

### 환경 제약
- 현재 환경에서는 cv2 미설치로 전체 python -m photo_cropper.selftest는 실패합니다.
- OpenCV 설치 후 전체 selftest를 재실행하면 추가 런타임 검증이 가능합니다.
